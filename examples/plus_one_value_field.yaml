label: plus-one-value-field
rules:
- target: records:creditcards.transactions
  action: read
  effect: allow
  transformations:
    - field: value
      function: plusOne
      where: "vendor == 'Kassulke Group'"
version: 1
