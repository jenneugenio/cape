version: 1
policy:
    rules:
        - target: "records:creditcards.transactions"
          action: read
          effect: allow
          fields:
              - vendor
              - value
              - timestamp
