// version contains build information that's set via ldflags during
// compilation.
package version

// Version contains the current build version
var Version = "dev"

// BuildDate contain the current build date as a string in UTC
var BuildDate = "never"
