kind: Service
apiVersion: v1
metadata:
  name: connector
  labels:
    service: connector
spec:
  ports:
  - name: "connector-api"
    port: {{ .Values.port }}
    nodePort: 30881
  selector:
    service: connector
  type: NodePort
status:
  loadBalancer: {}
