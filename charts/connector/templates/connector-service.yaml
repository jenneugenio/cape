kind: Service
apiVersion: v1
metadata:
  name: connector
  labels:
    service: connector
spec:
  ports:
  - name: "connector-api"
    port: 8081
    nodePort: 30881
  selector:
    service: connector
  type: NodePort
status:
  loadBalancer: {}
