kind: Service
apiVersion: v1
metadata:
  name: coordinator
  labels:
    service: coordinator
spec:
  ports:
  - name: "coordinator-api"
    port: 8080
    nodePort: 30880
  selector:
    service: coordinator
  type: NodePort
status:
  loadBalancer: {}
