kind: Service
apiVersion: v1
metadata:
  name: coordinator
  labels:
    service: coordinator
spec:
  ports:
  - name: "coordinator-api"
    port: {{ .Values.port }}
    nodePort: 30880
  selector:
    service: coordinator
  type: NodePort
status:
  loadBalancer: {}
