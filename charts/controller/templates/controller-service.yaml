kind: Service
apiVersion: v1
metadata:
  name: controller
  labels:
    service: controller
spec:
  ports:
  - name: "controller-api"
    port: 8081
  selector:
    service: controller
  type: NodePort
status:
  loadBalancer: {}