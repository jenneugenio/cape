name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build controller docker image
      run: docker build . --file Dockerfile --tag dropoutlabs/privacyai:$(date +%s)