label: masking_policy
version: 1
transformations:
  - name: hehehe
    type: reversible-tokenizer
    key:
      type: secret
      name: my-secret
rules:
  - match:
      name: name
    actions:
      - transform:
          type: hehehe
