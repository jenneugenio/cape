type Token {
    id: ID!
    user_id: ID!
}

type CreateTokenResponse {
    secret: Password!
    token: Token!
}

input CreateTokenRequest {
    user_id: ID!
}

extend type Query {
    tokens(user_id: ID!): [ID!]!
}

extend type Mutation {
    createToken(input: CreateTokenRequest!): CreateTokenResponse!
    removeToken(id: ID!): ID!
}
