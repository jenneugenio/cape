label: identity-card-number
rules:
- action: read
  effect: allow
  fields:
    - card_number
  target: records:creditcards.transactions
  sudo: true
  transformations:
    - field: card_number
      function: tokenization
      args:
        maxSize: 10
version: 1